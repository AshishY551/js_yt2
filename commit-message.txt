🔀 Add switch-case control flow examples (26switchcase.js)

- Implemented switch-case structure for clean conditional branching
- Demonstrated integer-based switch:
  • Example: month = 3 → outputs 'March'
  • Explained behavior when `break` is omitted (fall-through execution)
  • Highlighted execution flow after first match until a break is found

- Added string-based switch example:
  • Example: month2 = 'march' (case-sensitive check)
  • Illustrated mismatch due to lowercase vs uppercase ('march' vs 'March')
  • Default branch handling → outputs 'april'

- Key learnings captured:
  • `switch` is case-sensitive for string comparisons
  • Missing `break` executes subsequent cases (fall-through)
  • `default` acts as fallback when no match is found

✅ Enhances 04controlFlow module with practical switch-case scenarios  
✅ Demonstrates both numeric & string-based condition handling  
✅ Highlights common pitfalls (case sensitivity & missing break)
