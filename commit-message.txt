🔀 Add Control Flow examples with if-else & operators (26ifelse.js)

- Introduced basic control flow structures:
  • if, else-if, else statements
  • Inline if statements for compact syntax (with caveat on readability)

- Demonstrated key comparison & logical operators:
  • <, >, <=, >=, !=
  • == vs === (type coercion vs strict equality)
  • !== strict inequality check

- Showcased execution flow:
  • true/false conditions
  • Number vs string comparisons (2 == '2' ✅ / 2 === '2' ❌)
  • Logical AND (&&) and OR (||) combinations for login validation

- Explained scope handling:
  • let/const block scope (unavailable outside block)
  • var function/global scope (accessible outside block → unsafe)

- Added practical examples:
  • Temperature check (< 50 vs > 50)
  • Score & power variable scoping issue
  • Balance validation with multiple conditions
  • User authentication logic (login + debit card + multiple login methods)

✅ Enhances understanding of JS control flow  
✅ Covers operator differences with clear console outputs  
✅ Highlights best practices (avoid inline one-liners, prefer const/let over var)"

